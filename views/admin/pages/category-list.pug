extends ../layouts/default.pug
include ../mixins/change-status.pug

block main
  
  h1.box-title Category Management

  //- Section 4
  .section-4
    .inner-wrap
      .inner-item
        <i class="fa-solid fa-filter"></i> Filter
      .inner-item
        select(filter-status)
          option(value="") Status
          option(value="active") Active
          option(value="inactive") Inactive
      .inner-item
        select(filter-created-by)
          option(value="") Created By
          each item in accountAdminList
            option(value=item.id) #{item.fullName}
      .inner-item
        input(type="date" filter-start-date min="2000-01-01" max="2099-12-31")
        span -
        input(type="date" filter-end-date min="2000-01-01" max="2099-12-31")
      .inner-item.inner-reset(filter-reset)
        <i class="fa-solid fa-rotate-left"></i> Reset Filter
  //- End Section 4

  //- Section 5
  .section-5
    .inner-wrap
      +change-status(
        `/${pathAdmin}/category/change-multi`,
        [
          {
            label: "Active",
            value: "active"
          },
          {
            label: "Inactive",
            value: "inactive"
          },
          {
            label: "Delete",
            value: "delete"
          }
        ]
      )
      .inner-search
        <i class="fa-solid fa-magnifying-glass"></i>
          input(type="text" placeholder="Search" search)
      .inner-button-create
        a(href=`/${pathAdmin}/category/create`) + Create New
      .inner-button-trash
        a(href=`/${pathAdmin}/category/trash`) Trash
  //- End Section 5

  //- Section 6
  .section-6
    .table-2
      table
        thead
          tr
            th
              input(type="checkbox" class="inner-check" check-all)
            th Category Name
            th(class="inner-center") Avatar
            th(class="inner-center") Position
            th(class="inner-center") Status
            th Created By
            th Updated By
            th Action
        tbody
          if categoryList.length > 0
            each item in categoryList
              tr
                td
                  input(type="checkbox" class="inner-check" check-item=item.id)
                td #{item.name}
                td(class="inner-center")
                  img(src=item.avatar class="inner-avatar")
                td(class="inner-center") #{item.position}
                td(class="inner-center")
                  if (item.status == "active")
                    .badge.badge-green Active
                  else if (item.status == "inactive")
                    .badge.badge-red Inactive
                td
                  div #{item.createdByFullName}
                  div.inner-time #{item.createdAtFormat}
                td
                  div #{item.updatedByFullName}
                  div.inner-time #{item.updatedAtFormat}
                td
                  .inner-buttons
                    a(href=`/${pathAdmin}/category/edit/${item.id}` class="inner-edit")
                      <i class="fa-regular fa-pen-to-square"></i>
                    button(
                      class="inner-delete"
                      button-delete
                      data-api=`/${pathAdmin}/category/delete/${item.id}`
                      )
                      <i class="fa-regular fa-trash-can"></i>
          else
            tr
              td(colspan="9" style="text-align: center; padding: 40px 20px; color: #999;")
                | No categories found.
  //- End Section 6

  //- Section 7
  include ../partials/pagination.pug
  //- End Section 7